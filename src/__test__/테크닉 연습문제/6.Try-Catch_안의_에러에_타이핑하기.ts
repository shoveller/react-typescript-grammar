/**
 * 아래의 try ~ catch 문 안에서 타입 에러가 발생하고 있습니다.
 * 지금까지 배운 테크닉 중의 하나를 이용해서 에러를 해결해 보세요.
 * 키워드는 아래와 같습니다.
 * 1. any
 * 2. as
 * 3. instanceof
*/

try {
  // 중요한 일  
} catch(e) {
  console.log(e.message)
}