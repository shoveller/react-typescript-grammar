export {};

/**
 * 아래의 함수는 두 숫자를 더하는 함수입니다.
 * 아래의 함수에서 타입스크립트 에러를 없애 보세요.
 */
const 더하기 = (매개변수1, 매개변수2) => {
  return 매개변수1 + 매개변수2;
};
더하기(1, 2);

/**
 * 1. 이 에러는 타입을 추론하기 위한 정보가 부족해서 발생하는 것입니다.
 * 2. vscode의 자동수정 기능으로 에러를 수정하세요.
 */
